# なわとびスピード練習メトロノーム - 提案書

**現行版：Ver0.17 / 更新日：2026-09-08**

## 概要

なわとびのスピード競技（特に30秒スピード）向けに、時間帯ごとにBPMを変えられるメトロノームWebアプリ。
設定した競技ペースをブラウザ上で再生する。MP3出力はVer0.17で削除済み。
スマートフォン・PCどちらでも快適に使用可能。JP/EN 2言語対応。

---

## BPMの考え方

| なわとびの回数（回/分） | メトロノームBPM |
|---|---|
| 80回 | 160 BPM |
| 79回 | 158 BPM |
| 78回 | 156 BPM |
| 76回 | 152 BPM |

**計算式：BPM = 回数 × 2**

---

## 機能要件

### 1. 全体時間の設定
- 練習時間を自由に設定（デフォルト：30秒）
- 入力範囲：1秒〜1200秒

### 2. セグメント設定（タイムライン）
- 全体時間を任意の区間（セグメント）に分割できる
- セグメント数に制限なし（最低1つ）
- 各セグメントに以下を設定：
  - 開始時間（秒）
  - 終了時間（秒）
  - なわとびの回数（回/分）→ 内部でBPM変換
  - モード：**ステップ（即時切替）** または **グラデーション（徐々に変化）**
- 停止中にセグメントの追加・削除が可能。回数は30〜200、小数第1位まで指定可能。

**グラデーションモードの動作：**
- セグメント内で開始BPMから次セグメントのBPMへ線形補間
- 各ビートごとに次ビートまでの間隔を再計算してスケジューリング

**デフォルト設定（30秒スピード練習例）：**
```
0〜10秒:  80回/分 → BPM 160（ステップ）
10〜20秒: 70回/分 → BPM 140（ステップ）
20〜30秒: 60回/分 → BPM 120（ステップ）
```

### 3. アナウンス機能

**開始アナウンス（自由記述）：**
- 再生前に読み上げるテキストを自由入力（デフォルト：「Single Rope」）
- TTS（ブラウザの音声合成）で読み上げ完了後にカウントダウンへ移行

**カウントダウン：**
- 0〜10秒の整数で指定（デフォルト3秒）
- 例：3秒 → "Three, Two, One" と読み上げ → スタートBEEP → メトロノーム開始

**回数アナウンス：**
- セグメント開始・切り替わり時に回数を英語で読み上げ
- 例：「eighty」「seventy」

**時間アナウンス：**
- 任意の時間（秒）を複数指定（デフォルト：10秒・20秒）
- その時刻になったら経過秒数を英語で読み上げ

### 4. BEEP音
- **スタートBEEP**：最初のビートの合図（メトロノーム開始の瞬間）
- **切替BEEP**：セグメント切り替わりの合図
- **終了BEEP**：メトロノーム終了の合図
- 各BEEPは独立してON/OFF可能

### 5. 機能ON/OFFトグル（7項目）
| トグル | 内容 |
|---|---|
| 開始アナウンス読み上げ | 自由記述テキストのTTS読み上げ |
| カウントダウン音声 | "Three, Two, One" のTTS読み上げ |
| スタートBEEP | 最初のビートのBEEP音 |
| 切替BEEP | セグメント切り替わりのBEEP音 |
| 終了BEEP | メトロノーム終了のBEEP音 |
| 回数アナウンス | 回数の英語読み上げ |
| 時間アナウンス | 指定時刻の英語読み上げ |

### 6. クリック音の種類（5種）
- 電子音（デフォルト）
- 木琴風
- シンプル
- ウッド
- ハイハット

### 7. BEEP音量スライダー
- ゲイン調節：×0.5〜×5.0（デフォルト：×2.5）
- BEEP音に適用（クリック音のゲインは音色ごとの固定値）

### 8. リアルタイムプレビュー再生
- ブラウザ上でそのまま再生
- タイムラインバー上に再生位置インジケーターをリアルタイム表示
- 現在の回数・残り時間・セグメント番号を表示。グラデーション中は補間した回数・BPMを表示。
- 再生中は設定編集を無効化。停止時に予約音源・タイマー・古い非同期処理を解除する。

### 9. タイムライン（統合表示）
- 色付きセグメントバーとBPM形状ラインを1つに統合
- セグメントバーの色は回数に応じてHSLカラーで変化
- 白いライン（ステップ：水平線＋垂直線、グラデーション：斜め線）とドットをバー上にオーバーレイ表示

### 10. 設定の保存・読み込み
- localStorageで設定を自動保存し、設定コードのコピー・貼り付けにも対応。
- 時間アナウンスは1〜1200秒を保存。全体時間を超える指定は保持し、その再生では読み上げない。
- MP3ダウンロードのUI・関連コード・lamejs依存は削除済み。再実装の予定は未定。

### 11. 多言語対応（JP/EN）
- ヘッダーのトグルボタンで日本語／英語を切替
- `data-i18n` 属性ベースの軽量i18nシステム
- JS生成コンテンツ（セグメントカード・再生情報）も含めて全UI翻訳

### 12. 音声ファイルの作り方（ガイド）
- オプション下にアコーディオン形式で折りたたみ表示
- Windows の「ステレオミキサー＋サウンドレコーダー」を使った録音手順を JP/EN で掲載

---

## UI設計（スマホ対応・レスポンシブ）

縦1列レイアウト、ボタン大型化（最小高さ52px）、タッチ操作最適化。
最大幅680px、中央寄せ。スマホ・デスクトップで共通レイアウト。
オプションはアコーディオン（`<details>`）で折りたたみ表示。

---

## 技術スタック

| 技術 | 用途 |
|---|---|
| HTML / CSS / JavaScript | ベース（ビルドツール不要） |
| Web Audio API | リアルタイムメトロノーム再生・音声生成・コンプレッサー |
| Web Speech API | TTS（アナウンス・カウントダウン） |
| Canvas 2D API | タイムラインBPM形状ライン描画 |
| CSS Grid / Flexbox | レスポンシブレイアウト |

---

## ファイル構成

```
Speed_Practice_App/
├── index.html      # メインUI
├── style.css       # スタイル（レスポンシブ対応）
├── i18n.js         # 翻訳辞書・言語切替ロジック
├── app.js          # アプリロジック（State管理・UI・TTS）
├── metronome.js    # Web Audio APIによる再生エンジン
├── tests/regression.cjs # 回帰テスト（9件）
├── plan.md         # 本提案書
├── spec.md         # 詳細仕様書
└── dev-notes.md    # 開発ノート（実装上の知見・バグ記録）
```

---

*作成日: 2026-05-09*
*更新日: 2026-09-08（Ver0.17）*

## 現在の確認状況と保留事項

2026-09-08に公開版Ver0.17をChromeで確認済み。編集制限、停止・再再生、自然終了、設定の再読込、グラデーション表示、MP3 UI削除を確認した。回帰テスト9件と構文チェックは成功。実音のミリ秒精度・モバイル実機は未検証。

停止後の再生情報の表示残りは既知の不具合として保留。ユーザー判断で今回は追加修正せず一区切りとする。詳細は [spec.md](spec.md) 第15節と [dev-notes.md](dev-notes.md) のVer0.17検証記録を参照。
